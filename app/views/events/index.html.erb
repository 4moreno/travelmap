<div class="container mt-5">
  <h1 class="text-center">Calendar</h1>
  <%= month_calendar do |date| %>
    <%= date %>
  <% end %>
  <%= link_to "New event", new_event_path, class: "btn-peach" %>
</div>
